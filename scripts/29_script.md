# 第29回｜セキュリティ設定（RLS）を理解して設定する

**尺：12分 / Part7**
**ゴール：自分のデータだけ見える安全な設定ができる**
**収録スタイル：RLSポリシー設定から確認まで画面収録**

---

## 構成

| ブロック | 内容 | 時間 |
|----------|------|------|
| オープニング | なぜ今セキュリティを学ぶか | 1分 |
| 本編① | なぜセキュリティ設定が必要か | 3分 |
| 本編② | RLSとは何か | 3分 |
| 本編③ | 実演：RLSポリシーを設定する | 4分 |
| クロージング | まとめ・次回予告 | 1分 |

---

## トークスクリプト

### 【オープニング】（約1分）
*【画面：Supabaseダッシュボード】*

「これまで名刺の登録・一覧・検索・編集・削除と機能を作ってきました。しかし、実はまだ重大な問題が残っています。今のままでは、ログインしている全員が、他の人が登録した名刺も見えてしまう可能性があるのです。

今日は『RLS（Row Level Security）』という仕組みを使って、これを解決します。」

---

### 【本編①：なぜセキュリティ設定が必要か】（約3分）
*【画面：図解：複数ユーザーがアクセスするイメージ】*

「Supabaseのデータベースは、初期設定のままだと『誰でもすべてのデータにアクセスできる』状態になっていることがあります。

名刺管理アプリを1人で使うなら問題になりにくいですが、複数の会社・複数のユーザーに配布する場合、Aさんの名刺データがBさんから見えてしまうのは大問題です。お客様に届けるアプリとして、これは絶対に避けなければなりません。

このような『他人のデータが見えてしまう』『他人のデータを書き換えられてしまう』という問題を防ぐのが、今日学ぶセキュリティ設定の役割です。」

---

### 【本編②：RLSとは何か】（約3分）
*【画面：SupabaseのRLS設定ページ】*

「RLSは『Row Level Security（行レベルセキュリティ）』の略です。

データベースのテーブルの『行』、つまり1件1件のデータごとに『誰がこのデータを見られるか・編集できるか』を制御する仕組みです。

今回のアプリでは、第26回で説明したuser_id列が活躍します。『そのデータのuser_idが、今ログインしている人のIDと一致する場合だけ、見ることができる』というルール（ポリシー）を設定します。

これにより、Aさんがログインすると自分のuser_idのデータだけが見え、Bさんがログインすると別のデータだけが見える、という安全な状態になります。」

---

### 【本編③：実演：RLSポリシーを設定する】（約4分）
*【画面：Claude Code】*

「実際に設定しましょう。チャット欄に入力します。

『business_cardsテーブルにRow Level Security（RLS）を有効化し、ユーザーが自分のuser_idのデータだけをSELECT・INSERT・UPDATE・DELETEできるポリシーを作るSQLを書いてください』

生成されたSQLをコピーし、Supabaseの『SQL Editor』に貼り付けて実行します。

設定が終わったら、Supabaseダッシュボードの『Authentication』→『Policies』（または該当テーブルのRLSタブ）を開き、ポリシーが正しく登録されていることを確認します。

最後に、ブラウザで実際にアプリにログインし直し、一覧画面が今までと同じように自分のデータだけ表示されることを確認します。これで安全な状態になりました。」

---

### 【クロージング】（約1分）
*【画面：Supabaseのポリシー一覧画面】*

「まとめます。

- 何も設定しないと他人のデータが見えてしまう可能性がある
- RLSは行ごとにアクセス権限を制御する仕組み
- user_idを使って『自分のデータだけ』にアクセスを絞るポリシーを設定した

次回はいよいよVercelに公開し、他社にも配布できる状態にします。お疲れさまでした。」

---

## 操作・画面の流れ（詳細版）

### 📋 収録前準備チェックリスト
- [ ] 前回までの機能が実装済みのプロジェクトを開く
- [ ] Supabaseダッシュボードをブラウザのタブで開いておく
- [ ] できればテスト用に2つ目のユーザーアカウントを用意し、RLS設定前後の挙動を見せられるようにする（任意）

### 🎬 収録手順（12分完全版）

#### 【オープニング】 0:00〜1:00
| 経過時間 | 画面操作 | 話す内容 |
|---------|---------|---------|
| 0:00〜1:00 | Supabaseダッシュボード | 「今のままでは他人のデータが見える危険があります。今日はRLSで解決します」 |

#### 【本編①：なぜ必要か】 1:00〜4:00
| 経過時間 | 画面操作 | 話す内容 |
|---------|---------|---------|
| 1:00〜4:00 | 図解スライド（複数ユーザーアクセスのイメージ） | 「初期設定では誰でも全データにアクセスできる可能性があると説明。お客様に配布するなら絶対避けるべき問題と強調」 |

#### 【本編②：RLSとは何か】 4:00〜7:00
| 経過時間 | 画面操作 | 話す内容 |
|---------|---------|---------|
| 4:00〜7:00 | SupabaseのRLS設定ページを表示 | 「行レベルでアクセス制御する仕組み。user_id列を使って自分のデータだけ見えるようにする原理を説明」 |

#### 【本編③：RLS設定実演】 7:00〜11:00
| 経過時間 | 画面操作 | 話す内容 |
|---------|---------|---------|
| 7:00〜7:30 | Claude Codeチャット欄にRLSポリシー作成SQLの指示を入力してEnter | 「ポリシー作成のSQLを書いてもらいます」 |
| 7:30〜8:00 | 生成されたSQLをコピー | 「コピーします」 |
| 8:00〜9:00 | Supabase「SQL Editor」に貼り付けて「Run」をクリック | 「実行します」 |
| 9:00〜9:30 | Supabase該当テーブルの「RLS」タブでポリシー一覧を確認 | 「ポリシーが登録されました」 |
| 9:30〜11:00 | ブラウザでアプリに再ログインし、一覧画面が正しく自分のデータのみ表示されることを確認 | 「今まで通り自分のデータが見えています。安全な状態になりました」 |

#### 【クロージング】 11:00〜12:00
| 経過時間 | 画面操作 | 話す内容 |
|---------|---------|---------|
| 11:00〜12:00 | Supabaseのポリシー一覧画面 | 「次回はVercelに公開して配布できる状態にします。お疲れさまでした」 |

### ⚠️ 収録時の注意点
- RLSは概念が難しいので、図解をしっかり使い「鍵をかける」のようなたとえを使うとよい
- RLS設定前にうっかり本番データを使わないよう、テストデータのみで実演すること
