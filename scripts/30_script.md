# 第18回｜Word・PDF出力機能を追加して完成させる

**尺：12分 / Part4**
**ゴール：マニュアルをファイルとして保存・配布できる**
**収録スタイル：操作画面を映しながら音声解説**

---

## 構成

| ブロック | 内容 | 時間 |
|----------|------|------|
| オープニング | 前回の復習・この動画の目的 | 1分 |
| 本編① | Word出力機能を実装する | 4分 |
| 本編② | PDF出力機能を実装する | 4分 |
| 本編③ | 全体の通しテスト | 2分 |
| クロージング | まとめ・次回予告 | 1分 |

---

## トークスクリプト

### 【オープニング】（約1分）

「前回でステップの編集・削除・並び替えができるようになりました。今回はいよいよ最後の機能、Word・PDF出力機能を追加してアプリを完成させます。

出力ができると、作ったマニュアルをファイルとして保存して他の人に配布できるようになります。」

---

### 【本編①：Word出力機能を実装する】（約4分）

「*【画面収録開始】*

まずWord出力機能を実装します。

チャット欄に入力します。

『Word出力機能を実装してください。

・使用するライブラリ：python-docx
・まだインストールされていない場合はインストールもお願いします
・出力内容：
  - ドキュメントタイトル「操作マニュアル」を先頭に大きく表示
  - 各ステップを「Step 1：」「Step 2：」と番号つきで表示
  - 各ステップに画像を挿入（画像幅は12cm）
  - 画像の下に説明文を表示
・出力先：output/フォルダにファイル名は「manual_日時.docx」で保存
・保存完了後に「Word形式で保存しました：ファイルパス」とメッセージを表示』

---

実装完了後、動作確認をします。

「Word出力」ボタンをクリックします。

処理が完了してメッセージが表示されました。outputフォルダにdocxファイルが保存されています。

ファイルをダブルクリックしてWordで開きます。タイトル・ステップ番号・画像・説明文が整然と並んでいます。

*【画面収録終了】*」

---

### 【本編②：PDF出力機能を実装する】（約4分）

「*【画面収録開始】*

次にPDF出力機能を実装します。

チャット欄に入力します。

『PDF出力機能を実装してください。

・使用方法：作成したWordファイルをPDFに変換する、または直接PDFを生成する（どちらでも実装しやすい方法で）
・出力先：output/フォルダにファイル名は「manual_日時.pdf」で保存
・Word出力と同じレイアウトで出力する
・保存完了後にメッセージを表示』

---

実装完了後、「PDF出力」ボタンをクリックして動作確認します。

PDFファイルが生成されました。ブラウザかプレビューアプリで開いて内容を確認します。画像と説明文が正しく配置されています。

*【画面収録終了】*」

---

### 【本編③：全体の通しテスト】（約2分）

「*【画面収録開始】*

全機能が揃ったので、最初から最後まで通しテストをします。

1. アプリを起動
2. 実際の画面録画ファイルをドロップ
3. 処理開始をクリック（処理中はプログレスバーが進む）
4. プレビュー画面でステップを確認
5. 不要なステップを削除・説明文を修正
6. Word形式で出力

ファイルが生成されました。開いて確認すると、画面録画から自動的に作られたマニュアルが完成しています。

---

最後にGitでコミットします。

『今の変更をGitで保存してください。「アプリの全機能実装完了・通しテスト確認済み」というメッセージでコミットしてください』

*【画面収録終了】*」

---

### 【クロージング】（約1分）

「まとめます。

- Word出力はpython-docxライブラリを使って実装
- PDF出力も同様にライブラリを使って実装
- 全機能が揃い通しテストで動作確認できた

次回はエラーが出たときの対処法を学びます。

お疲れさまでした。」

---

## 操作・画面の流れ（詳細版）

### 📋 収録前準備チェックリスト
- [ ] manual_creatorプロジェクトを開く（編集機能実装済み）
- [ ] python-docx・reportlabがインストール済みか確認
- [ ] 画面録画を開始する（音声テスト済み）
- [ ] 通知をオフにする

### 🎬 収録手順（12分完全版）

#### 【オープニング】 0:00〜1:00
- 「今日は出力機能を実装してアプリを完成させます！Word出力とPDF出力の2つです」

#### 【本編①：Word出力機能を実装する】 1:00〜5:00
1. **1:00〜1:30** 「プレビュー画面に『Wordで出力』ボタンを追加してください。python-docxを使い画像つきの手順マニュアルを生成してください」と入力してEnter
2. **1:30〜3:00** 承認でYes → main.py・exporter.pyが更新される
3. **3:00〜4:00** 実行 → 動画をドロップ → プレビューを確認 → 「Wordで出力」をクリック
4. **4:00〜5:00** Wordファイルが生成される → ダブルクリックで開く → 画像つきマニュアルを映す

#### 【本編②：PDF出力機能を実装する】 5:00〜9:00
1. **5:00〜5:30** 「同様にPDFで出力ボタンも追加してください」と入力 → Yes
2. **5:30〜7:00** 実行 → 「PDFで出力」をクリック → PDFが生成される
3. **7:00〜9:00** PDFを開いて内容を確認・映す

#### 【本編③：全体の通しテスト】 9:00〜11:00
- 動画ドロップ → 画像生成 → AI説明文 → プレビュー → Word出力 → PDF出力を一通り実行して映す

#### 【クロージング】 11:00〜12:00
- 「アプリの実装が完成しました！次回から仕上げパートです。お疲れさまでした」


### ⚠️ 収録時の注意点
- 各操作後は2〜3秒間画面を止めて視聴者に確認の時間を与える
- ファイルが生成された瞬間・ブラウザで開いた瞬間など成果が見えるシーンはゆっくり映す
- 承認ダイアログは必ず3ボタンが見えるよう拡大表示してからYesをクリック
